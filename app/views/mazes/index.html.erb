<p id="notice"><%= notice %></p>

<h1>Listing Mazes</h1>

<table>
  <thead>
    <tr>
      <th>Height</th>
      <th>Width</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mazes.each do |maze| %>
      <tr>
        <td><%= maze.height %></td>
        <td><%= maze.width %></td>
        <td><%= link_to 'Show', maze %></td>
        <td><%= link_to 'Edit', edit_maze_path(maze) %></td>
        <td><%= link_to 'Destroy', maze, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Maze', new_maze_path %>
